# URL backend Laravel (dipakai server-side route handlers & client-side fetch)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Nama cookie httpOnly tempat menyimpan token Sanctum (Bearer token)
AUTH_COOKIE_NAME=pesantren_token
